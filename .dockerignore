.bundle
.env
.env.*
docker-compose.*
docker/Dockerfile
docker/dockerfiles
log
storage
public/system
tmp
.codeclimate.yml
public/packs
node_modules
vendor/bundle
.DS_Store
*.swp
*~

# PR audit 2026-05-22: CSV/XLSX de clientes reais usados pra testar o módulo
# Migration NUNCA devem ir pra imagem (LGPD: pacientes/CPF/telefone/anamnese
# de clientes reais — Streit, Mamedes, etc.). Devs reproduzem os imports
# localmente apontando pra `plugins/xls/` no host (bind mount). Em produção,
# super admin faz upload pela UI e o arquivo nem toca em disco do container.
plugins/xls/
plugins/xls/*
