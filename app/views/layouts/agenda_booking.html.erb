<%
  brand_name = (@brand_name.presence || GlobalConfigService.load('BRAND_NAME', 'Klivy'))
%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Agendamento Online – <%= brand_name %></title>
  <meta name="description" content="Agende sua consulta online de forma rápida e fácil.">
  <%= csrf_meta_tags %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    /* Brand color matches the dashboard's primary blue (Tailwind blue-500
       ≈ rgb(var(--blue-9))). Tokens are exposed as CSS custom properties
       so a future per-account override can ship by injecting a single
       <style> block from the controller without touching this file. */
    :root {
      --brand: #1f6cf2;
      --brand-dark: #1858d4;
      --brand-light: #e8f0ff;
      --brand-mid: #5b8dee;
      --success: #10b981;
      --error: #ef4444;
      --warning: #f59e0b;
      --bg: #f8fafc;
      --card: #ffffff;
      --border: #e2e8f0;
      --text: #1e293b;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --radius: 16px;
      --radius-sm: 10px;
      --shadow: 0 4px 24px rgba(0,0,0,.08);
      --shadow-sm: 0 1px 4px rgba(0,0,0,.06);
      --transition: .2s ease;
    }

    html, body {
      font-family: 'Inter', -apple-system, sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      font-size: 15px;
      line-height: 1.6;
    }

    /* ─── Layout ─── */
    .booking-page {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* Auditoria UX 2026-05-15: header compacto — logo à esquerda, avatar +
       nome do profissional + badge "Agenda disponível" à direita. Substitui
       o prof-card grande que tinha embaixo. */
    .booking-header {
      background: white;
      border-bottom: 1px solid var(--border);
      padding: 12px 24px;
      display: flex;
      align-items: center;
      gap: 12px;
      justify-content: space-between;
    }
    .booking-header-logo {
      height: 26px;
      width: auto;
      display: block;
    }
    .booking-header-brand {
      font-size: 16px;
      font-weight: 700;
      color: var(--brand);
      letter-spacing: -0.02em;
    }
    .booking-header-link {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      color: inherit;
    }
    .booking-header-prof {
      display: flex;
      align-items: center;
      gap: 10px;
      min-width: 0;
    }
    .booking-header-avatar {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background: var(--brand-light);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      font-weight: 700;
      color: var(--brand);
      overflow: hidden;
      flex-shrink: 0;
    }
    .booking-header-prof-info {
      display: flex;
      flex-direction: column;
      min-width: 0;
      line-height: 1.2;
    }
    .booking-header-prof-name {
      font-size: 14px;
      font-weight: 700;
      color: var(--text);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      max-width: 220px;
    }
    .booking-header-prof-badge {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 11px;
      font-weight: 600;
      color: var(--brand);
      margin-top: 2px;
    }
    .booking-header-prof-badge svg { width: 11px; height: 11px; }

    .booking-main {
      flex: 1;
      display: flex;
      justify-content: center;
      padding: 24px 16px 80px;
    }

    .booking-container {
      width: 100%;
      max-width: 740px;
    }

    /* ─── Steps ─── */
    .steps-bar {
      display: flex;
      gap: 0;
      margin-bottom: 24px;
    }
    .step-item {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
    }
    .step-item:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 14px;
      left: 50%;
      width: 100%;
      height: 2px;
      background: var(--border);
      z-index: 0;
    }
    .step-item.done:not(:last-child)::after { background: var(--brand); }
    .step-dot {
      width: 28px;
      height: 28px;
      border-radius: 50%;
      background: white;
      border: 2px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 11px;
      font-weight: 700;
      color: var(--text-muted);
      position: relative;
      z-index: 1;
      transition: var(--transition);
    }
    .step-item.active .step-dot {
      border-color: var(--brand);
      background: var(--brand);
      color: white;
    }
    .step-item.done .step-dot {
      border-color: var(--brand);
      background: var(--brand);
      color: white;
    }
    .step-label {
      font-size: 11px;
      color: var(--text-muted);
      margin-top: 6px;
      font-weight: 500;
    }
    .step-item.active .step-label { color: var(--brand); }

    /* ─── Card ─── */
    .card {
      background: white;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      border: 1px solid var(--border);
      overflow: hidden;
    }
    .card-header {
      padding: 20px 24px 16px;
      border-bottom: 1px solid var(--border);
    }
    .card-title {
      font-size: 17px;
      font-weight: 700;
      color: var(--text);
    }
    .card-sub {
      font-size: 13px;
      color: var(--text-muted);
      margin-top: 3px;
    }
    .card-body { padding: 24px; }

    /* ─── Form ─── */
    .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }
    .form-grid .full { grid-column: 1 / -1; }
    .field { display: flex; flex-direction: column; gap: 6px; }
    .field label {
      font-size: 13px;
      font-weight: 600;
      color: var(--text);
    }
    .field label .req { color: var(--error); margin-left: 2px; }
    .field input, .field select, .field textarea {
      padding: 11px 14px;
      border: 1.5px solid var(--border);
      border-radius: var(--radius-sm);
      font-size: 14px;
      font-family: inherit;
      color: var(--text);
      background: white;
      transition: var(--transition);
      outline: none;
    }
    .field textarea { resize: vertical; min-height: 80px; line-height: 1.5; }
    .field input:focus, .field select:focus, .field textarea:focus {
      border-color: var(--brand);
      box-shadow: 0 0 0 3px rgba(31, 108, 242, .15);
    }
    .field input.is-invalid {
      border-color: var(--error);
    }
    .field input.is-invalid:focus {
      box-shadow: 0 0 0 3px rgba(239, 68, 68, .15);
    }
    .field-error {
      font-size: 12px;
      color: var(--error);
      font-weight: 500;
      margin-top: 2px;
      display: none;
    }
    .field-error.show { display: block; }
    .field .hint {
      font-size: 11px;
      color: var(--text-light);
    }

    /* ─── Service Picker (Step 2 top) — auditoria UX 2026-05-15 ───
       Substituto custom do <select> nativo. Inspirado no FormSelect do
       beclinic_core (trigger arredondado, chevron animado, dropdown
       com items ricos: nome do serviço + duração + preço). */
    .svc-picker-wrap {
      display: flex;
      flex-direction: column;
      gap: 6px;
      margin-bottom: 24px;
      padding-bottom: 24px;
      border-bottom: 1px solid var(--border);
    }
    .svc-picker-label {
      font-size: 13px;
      font-weight: 600;
      color: var(--text);
    }
    .svc-picker { position: relative; }
    .svc-trigger {
      width: 100%;
      padding: 11px 14px;
      border: 1.5px solid var(--border);
      border-radius: var(--radius-sm);
      font-size: 14px;
      font-family: inherit;
      color: var(--text);
      background: white;
      transition: var(--transition);
      outline: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      text-align: left;
    }
    .svc-trigger:hover { border-color: var(--brand); }
    .svc-trigger:focus,
    .svc-trigger--open {
      border-color: var(--brand);
      box-shadow: 0 0 0 3px rgba(31, 108, 242, .15);
    }
    .svc-trigger-text {
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .svc-trigger-text--placeholder { color: var(--text-light); }
    .svc-trigger-chevron {
      color: var(--text-muted);
      transition: transform 0.2s ease;
      flex-shrink: 0;
    }
    .svc-trigger--open .svc-trigger-chevron { transform: rotate(180deg); }

    .svc-dropdown {
      position: absolute;
      top: calc(100% + 6px);
      left: 0;
      right: 0;
      background: white;
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      box-shadow: 0 8px 24px rgba(15, 23, 42, 0.12);
      z-index: 50;
      max-height: 320px;
      overflow: hidden;
      animation: svcDropFadeIn 0.12s ease-out;
    }
    .svc-dropdown[hidden] { display: none; }
    @keyframes svcDropFadeIn {
      from { opacity: 0; transform: translateY(-4px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    .svc-dropdown-list {
      max-height: 320px;
      overflow-y: auto;
      padding: 4px;
    }
    .svc-option {
      width: 100%;
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 12px;
      border: none;
      background: transparent;
      border-radius: 6px;
      cursor: pointer;
      text-align: left;
      font-family: inherit;
      transition: background 0.12s;
    }
    .svc-option:hover { background: rgba(31, 108, 242, 0.06); }
    .svc-option--selected { background: rgba(31, 108, 242, 0.08); }
    .svc-option--selected .svc-option-title { color: var(--brand); }
    .svc-option--skip {
      border-bottom: 1px solid var(--border);
      border-radius: 6px 6px 0 0;
      margin-bottom: 4px;
      padding-bottom: 12px;
    }
    .svc-option-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      flex-shrink: 0;
    }
    .svc-option-icon {
      width: 28px;
      height: 28px;
      border-radius: 50%;
      background: var(--brand-light);
      color: var(--brand);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .svc-option-body {
      display: flex;
      flex-direction: column;
      gap: 2px;
      flex: 1;
      min-width: 0;
    }
    .svc-option-title {
      font-size: 14px;
      font-weight: 600;
      color: var(--text);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .svc-option-sub {
      font-size: 12px;
      color: var(--text-muted);
    }
    .svc-picker-hint {
      font-size: 11px;
      color: var(--text-light);
    }

    /* ─── Calendar ─── */
    .cal-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }
    .cal-month-nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 16px;
    }
    .cal-month-title {
      font-size: 15px;
      font-weight: 700;
      text-transform: capitalize;
    }
    .cal-nav-btn {
      width: 32px;
      height: 32px;
      border: 1px solid var(--border);
      border-radius: 8px;
      background: white;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
      transition: var(--transition);
    }
    .cal-nav-btn:hover { border-color: var(--brand); color: var(--brand); }
    .cal-weekdays {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      text-align: center;
      margin-bottom: 8px;
    }
    .cal-weekdays span {
      font-size: 11px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      padding: 4px 0;
    }
    .cal-days {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 4px;
    }
    .cal-day {
      aspect-ratio: 1;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 13px;
      font-weight: 500;
      cursor: pointer;
      transition: var(--transition);
      border: 1.5px solid transparent;
    }
    .cal-day.empty { cursor: default; }
    .cal-day.today { border-color: var(--brand); color: var(--brand); font-weight: 700; }
    .cal-day.available:hover { background: var(--brand-light); color: var(--brand); }
    .cal-day.selected { background: var(--brand); color: white; border-color: var(--brand); }
    .cal-day.past, .cal-day.unavailable {
      color: var(--text-light);
      cursor: not-allowed;
    }
    .cal-day.loading { animation: pulse 1.5s infinite; background: var(--bg); }

    /* 9.19 da auditoria — honeypot anti-bot. Off-screen (não display:none —
       bots detectam display:none e pulam o campo). Posição absoluta fora
       da viewport + height:0 + opacity:0 garante invisibilidade visual
       sem revelar a intenção ao bot. */
    .hp-field {
      position: absolute;
      left: -10000px;
      top: auto;
      width: 1px;
      height: 1px;
      overflow: hidden;
      opacity: 0;
    }

    /* ─── Time Slots ─── */
    .slots-label {
      font-size: 14px;
      font-weight: 600;
      color: var(--text);
      margin-bottom: 4px;
    }
    /* 9.18 da auditoria — fuso explícito (Brasília) para evitar confusão
       de pacientes em outros estados / com VPN. Discreto, abaixo do label. */
    .slots-timezone {
      font-size: 12px;
      color: var(--text-muted, #6b7280);
      margin-bottom: 12px;
    }
    .slots-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 8px;
    }
    .slot-btn {
      padding: 10px 6px;
      border: 1.5px solid var(--border);
      border-radius: var(--radius-sm);
      background: white;
      font-size: 13px;
      font-weight: 600;
      color: var(--text);
      cursor: pointer;
      text-align: center;
      transition: var(--transition);
      font-family: inherit;
    }
    .slot-btn:hover { border-color: var(--brand); color: var(--brand); background: var(--brand-light); }
    .slot-btn.selected { background: var(--brand); color: white; border-color: var(--brand); }
    .slots-empty {
      text-align: center;
      padding: 32px 16px;
      color: var(--text-muted);
      font-size: 13px;
    }
    .slots-loading {
      text-align: center;
      padding: 24px;
      color: var(--text-muted);
      font-size: 13px;
    }

    /* ─── Buttons ─── */
    .btn-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 24px;
      gap: 12px;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 24px;
      border-radius: var(--radius-sm);
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      transition: var(--transition);
      font-family: inherit;
    }
    .btn-primary {
      background: var(--brand);
      color: white;
    }
    .btn-primary:hover { background: var(--brand-dark); }
    .btn-primary:disabled { background: var(--text-light); cursor: not-allowed; }
    .btn-ghost {
      background: transparent;
      color: var(--text-muted);
      border: 1.5px solid var(--border);
    }
    .btn-ghost:hover { border-color: var(--brand); color: var(--brand); }

    /* ─── Success ─── */
    .success-card {
      text-align: center;
      padding: 56px 32px 48px;
      position: relative;
      overflow: hidden;
    }
    .success-icon {
      width: 84px;
      height: 84px;
      background: #ecfdf5;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      position: relative;
      animation: successPop .55s cubic-bezier(.34,1.56,.64,1) both;
    }
    /* Soft halo ring expanding out of the icon on first paint */
    .success-icon::before {
      content: '';
      position: absolute;
      inset: -8px;
      border-radius: 50%;
      border: 3px solid rgba(16, 185, 129, .35);
      animation: successHalo 1.4s ease-out .15s both;
    }
    .success-icon svg { width: 44px; height: 44px; color: var(--success); }
    .success-icon-ring {
      stroke-dasharray: 64;
      stroke-dashoffset: 64;
      animation: successDraw .7s ease-out .25s forwards;
    }
    .success-icon-check {
      stroke-dasharray: 32;
      stroke-dashoffset: 32;
      animation: successDraw .45s ease-out .55s forwards;
    }
    .success-title {
      font-size: 24px;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 10px;
      animation: successFadeIn .4s ease-out .35s both;
    }
    .success-emoji {
      display: inline-block;
      animation: successWiggle 1s ease-in-out .9s 2;
      transform-origin: 70% 70%;
    }
    .success-sub {
      font-size: 14px;
      color: var(--text-muted);
      max-width: 380px;
      margin: 0 auto 28px;
      line-height: 1.6;
      animation: successFadeIn .4s ease-out .45s both;
    }
    .success-detail {
      animation: successFadeIn .5s ease-out .6s both;
      display: inline-block;
    }
    /* Two-column "data + horário" pill, replacing the single text line */
    .success-pill {
      display: inline-flex;
      align-items: center;
      gap: 16px;
      background: var(--brand-light);
      border: 1px solid rgba(31, 108, 242, .15);
      border-radius: 14px;
      padding: 14px 22px;
      box-shadow: 0 4px 14px rgba(31, 108, 242, .12);
    }
    .success-pill-item {
      display: flex;
      align-items: center;
      gap: 12px;
      text-align: left;
      color: var(--brand);
    }
    .success-pill-label {
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .06em;
      color: var(--brand-mid);
      line-height: 1;
      margin-bottom: 4px;
    }
    .success-pill-value {
      font-size: 16px;
      font-weight: 700;
      color: var(--brand-dark);
      line-height: 1.1;
      font-variant-numeric: tabular-nums;
    }
    .success-pill-divider {
      width: 1px;
      height: 32px;
      background: rgba(31, 108, 242, .25);
    }
    /* 9.11 — bloco de cancelamento no step 4. Botão discreto (ghost com
       borda) pra não competir com a mensagem de sucesso, mas acessível
       quando o paciente quer cancelar. */
    .success-cancel {
      margin-top: 24px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 8px;
    }
    .btn-cancel-booking {
      color: #ef4444;
      border: 1px solid rgba(239, 68, 68, 0.4);
      background: transparent;
      padding: 8px 16px;
      border-radius: 8px;
      font-size: 13px;
      cursor: pointer;
      transition: background 0.15s, border-color 0.15s;
    }
    .btn-cancel-booking:hover:not(:disabled) {
      background: rgba(239, 68, 68, 0.08);
      border-color: rgba(239, 68, 68, 0.6);
    }
    .btn-cancel-booking:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    .success-cancel-msg {
      font-size: 13px;
      min-height: 18px;
    }
    .cancel-msg--success { color: #16a34a; }
    .cancel-msg--error   { color: #ef4444; }

    @keyframes successPop {
      0%   { transform: scale(.4); opacity: 0; }
      60%  { transform: scale(1.08); opacity: 1; }
      100% { transform: scale(1); opacity: 1; }
    }
    @keyframes successHalo {
      0%   { transform: scale(.6); opacity: .9; }
      100% { transform: scale(1.6); opacity: 0; }
    }
    @keyframes successDraw {
      to { stroke-dashoffset: 0; }
    }
    @keyframes successFadeIn {
      from { opacity: 0; transform: translateY(6px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes successWiggle {
      0%, 100% { transform: rotate(0); }
      25%      { transform: rotate(-12deg); }
      50%      { transform: rotate(10deg); }
      75%      { transform: rotate(-6deg); }
    }
    @media (prefers-reduced-motion: reduce) {
      .success-icon,
      .success-icon::before,
      .success-icon-ring,
      .success-icon-check,
      .success-title,
      .success-emoji,
      .success-sub,
      .success-detail {
        animation: none !important;
        stroke-dashoffset: 0 !important;
      }
    }

    /* ─── Error / Disabled ─── */
    .booking-error {
      text-align: center;
      padding: 80px 24px;
    }
    .booking-error-icon {
      font-size: 48px;
      margin-bottom: 16px;
    }
    .booking-error h2 { font-size: 20px; font-weight: 700; margin-bottom: 8px; }
    .booking-error p { font-size: 14px; color: var(--text-muted); }

    /* ─── Spinner ─── */
    .spinner {
      width: 20px; height: 20px;
      border: 2.5px solid rgba(255,255,255,.4);
      border-top-color: white;
      border-radius: 50%;
      animation: spin .7s linear infinite;
      display: inline-block;
    }
    .spinner-brand {
      border: 2.5px solid var(--brand-light);
      border-top-color: var(--brand);
    }
    @keyframes spin { to { transform: rotate(360deg); } }
    @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.4} }

    /* ─── Footer ─── */
    .booking-footer {
      text-align: center;
      padding: 20px;
      font-size: 12px;
      color: var(--text-light);
    }
    .booking-footer a { color: var(--brand); text-decoration: none; font-weight: 600; }

    /* Visually-hidden helper for ARIA-only labels */
    .sr-only {
      position: absolute;
      width: 1px; height: 1px;
      padding: 0; margin: -1px;
      overflow: hidden; clip: rect(0,0,0,0);
      white-space: nowrap; border: 0;
    }

    /* ─── Responsive ─── */
    @media (max-width: 768px) {
      .booking-main { padding: 24px 12px 60px; }
      .cal-grid { grid-template-columns: 1fr; gap: 24px; }
      .slots-grid { grid-template-columns: repeat(4, 1fr); }
    }
    @media (max-width: 600px) {
      .form-grid { grid-template-columns: 1fr; }
      .slots-grid { grid-template-columns: repeat(3, 1fr); gap: 6px; }
      /* Mobile: o header compacto encolhe — esconde nome do profissional
         para sobrar largura. Avatar + badge "Agenda disponível" continuam
         visíveis. Decisão de UX: avatar dá identidade visual sem precisar
         do nome em telas estreitas. */
      .booking-header { padding: 10px 16px; }
      .booking-header-prof-name { display: none; }
      .booking-header-prof-badge { font-size: 10px; }
      .card-body { padding: 20px 16px; }
      .step-dot { width: 32px; height: 32px; font-size: 13px; }
      .btn { padding: 14px 20px; font-size: 15px; }
      .btn-row { flex-direction: column-reverse; align-items: stretch; }
      .btn-row .btn { width: 100%; justify-content: center; }
      .btn-row > span { text-align: center; margin-top: 8px; }
    }
  </style>
</head>
<body>
  <div id="booking-app" class="booking-page">
    <%= yield %>
  </div>
  <footer class="booking-footer">
    Powered by <a href="<%= GlobalConfigService.load('BRAND_URL', 'https://klivy.com.br') %>" target="_blank" rel="noopener"><%= brand_name %></a>
  </footer>
</body>
</html>
