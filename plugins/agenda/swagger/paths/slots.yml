get:
  tags:
    - Agenda
  summary: Lista slots disponíveis
  description: Retorna uma lista de horários disponíveis para agendamento.
  responses:
    '200':
      description: Lista de slots retornada com sucesso
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/Slot'
