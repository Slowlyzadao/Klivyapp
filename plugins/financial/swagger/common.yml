components:
  parameters:
    accountId:
      name: account_id
      in: path
      required: true
      schema:
        type: integer
    id:
      name: id
      in: path
      required: true
      schema:
        type: integer
    patientId:
      name: patient_id
      in: path
      required: true
      schema:
        type: integer
    categoryId:
      name: category_id
      in: path
      required: true
      schema:
        type: integer
    treatmentPlanId:
      name: treatment_plan_id
      in: path
      required: true
      schema:
        type: integer
