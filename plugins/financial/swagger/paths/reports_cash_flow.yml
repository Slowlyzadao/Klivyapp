get:
  tags:
    - Financeiro · Relatórios
  summary: Relatório de Fluxo de Caixa
  parameters:
    - $ref: '../common.yml#/components/parameters/accountId'
    - name: from
      in: query
      schema: { type: string, format: date }
    - name: to
      in: query
      schema: { type: string, format: date }
    - name: bank_account_id
      in: query
      schema: { type: integer }
  responses:
    '200':
      description: Payload do fluxo de caixa (estrutura do `CashFlowReport`)
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
