components:
  parameters:
    accountId:
      name: account_id
      in: path
      required: true
      schema:
        type: integer
    id:
      name: id
      in: path
      required: true
      schema:
        type: integer
    roomId:
      name: room_id
      in: path
      required: true
      schema:
        type: integer
