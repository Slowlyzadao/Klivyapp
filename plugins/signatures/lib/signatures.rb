# frozen_string_literal: true

# Entry point do plugin signatures. Carregado automaticamente pelo Rails
# via glob em config/application.rb (`plugins/*/lib/*/engine.rb`).

require 'signatures/engine'

module Signatures
end
